#include<stdlib.h>
#include<stdio.h>
//#include<assert.h>
#include<math.h>

#define uint unsigned int

#define ea

typedef struct node {
  char operator
  struct node *next;
  struct node *prev;
} node;


typedef struct list{
  node *head;
  node *tail;
} list;

